<div>

	<table border=1>
	<tr>
		<td>&nbsp;</td>
		<? foreach($related_products as $rp) { ?>
		<th>
			<?= $rp['Product']['pricing_name'] ?>
		</th>
		<? } ?>
	</tr>
	<tbody id="features">
	<tr>
		<? for ($i = 0; $i < count($options); $i++) { ?>
		<? } ?>
	</tr>
	</tbody>
	</table>
</div>
